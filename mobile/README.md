# Mobile App

This directory contains the Expo mobile client for the Duck project.

Current scope:

- iPhone-first MVP
- Expo + React Native
- EAS for iOS cloud builds
- Local-first data strategy

Common commands:

```bash
npm start
npm run web
```

Next setup steps:

1. Add `expo-router`
2. Create tab navigation
3. Add local SQLite storage
4. Build the first novel list and create flow
